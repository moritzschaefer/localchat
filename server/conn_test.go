package main

import "testing"

func TestAddUser(t *testing.T) {
	c := Connection{}
	um.addUser(c)
	// if c not in um.users {
	// 	t.Errorf("addUser doesn't create user")
	// }
}
func TestUpdateUser(t *testing.T) {

}
